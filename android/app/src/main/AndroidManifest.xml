<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- VPNService 必需权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Mirage"
        android:supportsRtl="true"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true">

        <!-- 主界面 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- 支持 mirage:// 链接打开 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="mirage" />
            </intent-filter>
        </activity>

        <!-- VPN 服务 (独立内核进程 :core) -->
        <service
            android:name=".CoreService"
            android:exported="false"
            android:foregroundServiceType="specialUse|systemExempted|connectedDevice"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:process=":core">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Mirage VPN Proxy Tunnel" />
        </service>

        <!-- 通知栏快捷动作广播接收器 (与 CoreService 同进程) -->
        <receiver
            android:name=".receiver.CoreActionReceiver"
            android:process=":core"
            android:exported="true">
            <intent-filter>
                <action android:name="com.mirage.android.STOP" />
            </intent-filter>
        </receiver>

        <!-- 下拉控制中心快捷设置磁贴 (Quick Settings Tile) -->
        <service
            android:name=".service.MirageTileService"
            android:icon="@drawable/ic_notification_mirage"
            android:label="Mirage VPN"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.TileService" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/car_app" />
    </application>
</manifest>
